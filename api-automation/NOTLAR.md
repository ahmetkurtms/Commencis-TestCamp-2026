# api-automation

CRUD: liste, POST, GET, PUT (random first/last), GET, DELETE, GET 404.

Varsayılan: `mock-server.cjs` Playwright ile birlikte kalkıyor (port 9999). Başka bir API için `API_BASE_URL` ver; sonda `/` olsun, örn. `https://x.com/api/`

Postman ref: https://documenter.getpostman.com/view/4012288/TzK2bEa8
