{
  "name": "commencis-ui-test-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Commencis website GUI tests (Playwright + TypeScript)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report test-results/playwright-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.3"
  }
}
